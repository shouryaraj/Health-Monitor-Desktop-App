/**
 * Interface for a container of a certain object which creates an iterator for itself
 */
package Model;
public interface Container {
    Iterator createIterator(); // method to create Iterator
}
